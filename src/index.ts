import { h, render } from 'preact';
// import {
//     type StateUpdater,
//     useState,
// } from 'preact/hooks';


function App() {
    return h('h1', null, 'niggers!');
};

render(
    h(App, null),
    document.body
);
